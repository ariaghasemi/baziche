<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme (Compose takes over immediately in MainActivity). -->
    <style name="Theme.Baziche" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
